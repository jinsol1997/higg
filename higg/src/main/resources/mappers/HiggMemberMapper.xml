<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lol.higg.mapper.HiggMemberMapper">


    <insert id="insertMember" parameterType="com.lol.higg.dto.HiggMemberDTO">
        insert into higg_member (uid, pw) values (#{uid}, #{pw})
    </insert>


</mapper>