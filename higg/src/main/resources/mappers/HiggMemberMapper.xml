<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lol.higg.mapper.HiggMemberMapper">


    <insert id="insertMember" parameterType="com.lol.higg.dto.member.HiggMemberDTO">
        insert into higg_member (uid, pw, salt) values (#{uid}, #{pw}, #{salt})
    </insert>

    <select id="selectByIdPw" resultType="com.lol.higg.dto.member.HiggMemberDTO">
        select * from higg_member where uid=#{param1} and pw=#{param2}
    </select>


</mapper>